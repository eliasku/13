{
  "name": "@eliasku/iioi-autoplay",
  "version": "1.0.14",
  "description": "Default bot for Player autoplay, used mostly for testing",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --minify --bundle --mangle-props=^_[a-z] --platform=browser --target=es2020 --format=esm --outfile=../../public/autoplay.js",
    "pretypecheck": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "author": "eliasku",
  "license": "ISC",
  "dependencies": {
    "@eliasku/iioi-bot-api": "1.0.14"
  }
}
